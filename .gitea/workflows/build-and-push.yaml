name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: docker:stable-dind

    steps:
    - name: Set up Docker-in-Docker
      uses: docker/setup-buildx-action@v2

    - name: Install Curl, Node.js, and Yarn
      run: |
        apt-get update
        apt-get install -y curl nodejs npm yarn
        node -v
        npm -v
        yarn -v
        docker -v

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Verify Node.js and Yarn Installation
      run: |
        node -v
        npm -v
        yarn -v
        which node
        which npm
        which yarn

    - name: Install dependencies
      run: yarn install

    - name: Build project
      run: yarn build

    - name: Log in to Gitea Docker Registry
      uses: docker/login-action@v2
      with:
        registry: git.dreamfriday.com
        username: ${{ secrets.GITEAUSER }}
        password: ${{ secrets.GITEAPASSWORD }}

    - name: Build and push Docker image
      run: |
        docker build -t git.dreamfriday.com/${{ secrets.GITEAUSER }}/payload:latest .
        docker push git.dreamfriday.com/${{ secrets.GITEAUSER }}/payload:latest

    - name: Verify Docker Daemon Connection
      run: |
        docker info
